saldo = 1000
limite = 1000

print(f'saldo e limete ocupam o mesmo espaço de memória? {saldo is limite}')
print(f'saldo e limite ocupam o mesmo espaço de memória (negação)? {saldo is not limite}')

nome = 'Jhon'
sobrenome = 'Doe'

print(f'nome e sobrenome ocupam o mesmo espaço de memória? {nome is sobrenome}')
print(f'nome e sobrenome ocupam o mesmo espaço de memória (negação)? {nome is not sobrenome}')